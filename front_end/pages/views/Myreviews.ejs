<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>My Gym Reviews</title>
  <link rel="stylesheet" href="../../css/reviews.css" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&family=Nunito+Sans:wght@400;500;600;700&display=swap"
    rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <style>
    /* Brand Colors and Reset */
    :root {
      --primary-dark: #0e1428;
      --primary-light: #1a2040;
      --accent-orange: #e85d04;
      --accent-light: #f76707;
      --light-bg: #f8f9fa;
      --white: #ffffff;
      --gray-lightest: #f8f9fa;
      --gray-light: #e9ecef;
      --gray-medium: #6c757d;
      --gray-dark: #343a40;
      --text-primary: #0e1428;
      --text-secondary: #6c757d;
      --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.08);
      --shadow-md: 0 4px 16px rgba(0, 0, 0, 0.12);
      --shadow-lg: 0 8px 32px rgba(0, 0, 0, 0.16);
      --shadow-orange: 0 4px 20px rgba(232, 93, 4, 0.15);
      --gradient-primary: linear-gradient(135deg, var(--primary-dark) 0%, var(--primary-light) 100%);
      --gradient-orange: linear-gradient(135deg, var(--accent-orange) 0%, var(--accent-light) 100%);
      --gradient-subtle: linear-gradient(135deg, #fefdf8 0%, var(--gray-lightest) 100%);
      --border-radius: 16px;
      --border-radius-lg: 20px;
      --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      --font-heading: "Poppins", sans-serif;
      --font-body: "Nunito Sans", sans-serif;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: var(--font-body);
      background: var(--light-bg);
      color: var(--text-primary);
      line-height: 1.6;
      overflow-x: hidden;
    }

    /* Page Header */
    .page-header {
      background: var(--gradient-primary);
      padding: 2.5rem 2rem;
      margin-bottom: 2rem;
      border-bottom: 4px solid var(--accent-orange);
      position: relative;
      overflow: hidden;
    }

    .page-header::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grain" width="100" height="100" patternUnits="userSpaceOnUse"><circle cx="10" cy="10" r="1" fill="rgba(255,255,255,0.05)"/><circle cx="40" cy="40" r="1.5" fill="rgba(255,255,255,0.03)"/><circle cx="70" cy="20" r="1" fill="rgba(255,255,255,0.04)"/><circle cx="20" cy="70" r="1.2" fill="rgba(255,255,255,0.02)"/></pattern></defs><rect width="100" height="100" fill="url(%23grain)"/></svg>');
      opacity: 0.6;
    }

    .header-content {
      max-width: 1200px;
      margin: 0 auto;
      display: flex;
      align-items: center;
      gap: 1.5rem;
      position: relative;
      z-index: 2;
    }

    .header-icon {
      width: 80px;
      height: 80px;
      background: var(--gradient-orange);
      border-radius: 20px;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: var(--shadow-orange);
      animation: float 3s ease-in-out infinite;
    }

    .header-icon i {
      font-size: 2rem;
      color: var(--white);
    }

    @keyframes float {

      0%,
      100% {
        transform: translateY(0px);
      }

      50% {
        transform: translateY(-10px);
      }
    }

    .header-text h1 {
      font-family: var(--font-heading);
      font-size: 3rem;
      font-weight: 700;
      color: var(--white);
      margin-bottom: 0.5rem;
      text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
    }

    .header-text p {
      font-size: 1.125rem;
      color: rgba(255, 255, 255, 0.9);
      font-weight: 500;
    }

   

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(40px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Reviews Container */
    #reviews {
      background: var(--white);
      border-radius: var(--border-radius-lg);
      padding: 3rem;
      box-shadow: var(--shadow-lg);
      border: 1px solid var(--gray-light);
      position: relative;
      overflow: hidden;
    }

    #reviews::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 6px;
      background: var(--gradient-orange);
    }

    /* Stats Header */
    .reviews-stats {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1.5rem;
      margin-bottom: 3rem;
      padding: 2rem;
      background: var(--gradient-subtle);
      border-radius: var(--border-radius);
      border: 2px solid var(--gray-light);
    }

    .stat-item {
      text-align: center;
      padding: 1.5rem;
      background: var(--white);
      border-radius: 12px;
      box-shadow: var(--shadow-sm);
      transition: var(--transition);
      border: 1px solid var(--gray-light);
    }

    .stat-item:hover {
      transform: translateY(-4px);
      box-shadow: var(--shadow-md);
      border-color: var(--accent-orange);
    }

    .stat-number {
      font-family: var(--font-heading);
      font-size: 2.5rem;
      font-weight: 700;
      color: var(--accent-orange);
      display: block;
      margin-bottom: 0.5rem;
    }

    .stat-label {
      font-size: 0.95rem;
      color: var(--text-secondary);
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    /* Reviews List */
    #review-list {
      display: grid;
      gap: 1.5rem;
    }

    /* Individual Review Cards */
    .review {
      background: var(--white);
      border-radius: var(--border-radius);
      padding: 2rem;
      box-shadow: var(--shadow-sm);
      border: 2px solid var(--gray-light);
      transition: var(--transition);
      position: relative;
      overflow: hidden;
      animation: slideInUp 0.6s ease-out forwards;
      opacity: 0;
      transform: translateY(30px);
    }

    .review:nth-child(odd) {
      animation-delay: 0.1s;
    }

    .review:nth-child(even) {
      animation-delay: 0.2s;
    }

    @keyframes slideInUp {
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .review::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 6px;
      height: 100%;
      background: var(--gradient-orange);
      transition: width 0.3s ease;
    }

    .review:hover {
      transform: translateY(-8px);
      box-shadow: var(--shadow-lg);
      border-color: var(--accent-orange);
    }

    .review:hover::before {
      width: 12px;
    }

    /* Review Header */
    .review-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 1.5rem;
      padding-bottom: 1rem;
      border-bottom: 2px solid var(--gray-light);
    }

    .review-author {
      display: flex;
      align-items: center;
      gap: 1rem;
    }

    .author-avatar {
      width: 50px;
      height: 50px;
      background: var(--gradient-orange);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--white);
      font-family: var(--font-heading);
      font-weight: 600;
      font-size: 1.25rem;
      box-shadow: var(--shadow-sm);
    }

    .author-info h4 {
      font-family: var(--font-heading);
      font-size: 1.125rem;
      font-weight: 600;
      color: var(--text-primary);
      margin-bottom: 0.25rem;
    }

    .author-info .review-date {
      font-size: 0.875rem;
      color: var(--text-secondary);
      font-weight: 500;
    }

    /* Rating Display */
    .review-rating {
      display: flex;
      align-items: center;
      gap: 0.5rem;
      font-size: 1.5rem;
    }

    .review-rating .stars {
      color: #ffd700;
      text-shadow: 0 1px 3px rgba(255, 215, 0, 0.4);
      filter: drop-shadow(0 0 4px rgba(255, 215, 0, 0.3));
    }

    .review-rating .empty-stars {
      color: var(--gray-light);
    }

    .rating-number {
      font-family: var(--font-heading);
      font-weight: 700;
      color: var(--accent-orange);
      font-size: 1.25rem;
      margin-left: 0.5rem;
    }

    /* Comment Text */
    .review-comment {
      font-size: 1.125rem;
      line-height: 1.8;
      color: var(--text-primary);
      padding: 1.5rem;
      background: var(--gradient-subtle);
      border-radius: 12px;
      border-left: 4px solid var(--accent-orange);
      position: relative;
      margin-top: 1rem;
    }

    .review-comment::before {
      content: '\201C';
      font-size: 3rem;
      color: var(--accent-orange);
      position: absolute;
      top: -5px;
      left: 10px;
      opacity: 0.3;
      font-family: Georgia, serif;
    }

    .review-comment p {
      margin: 0;
      position: relative;
      z-index: 1;
    }

    /* Empty State */
    .empty-state {
      text-align: center;
      padding: 4rem 2rem;
      background: var(--gradient-subtle);
      border-radius: var(--border-radius);
      border: 2px dashed var(--gray-light);
      margin: 2rem 0;
    }

    .empty-state-icon {
      font-size: 4rem;
      color: var(--gray-light);
      margin-bottom: 1.5rem;
      display: block;
    }

    .empty-state h3 {
      font-family: var(--font-heading);
      font-size: 1.5rem;
      color: var(--text-secondary);
      margin-bottom: 1rem;
    }

    .empty-state p {
      font-size: 1.125rem;
      color: var(--text-secondary);
      line-height: 1.6;
    }

    /* Error Message Styling */
    .error-message {
      text-align: center;
      padding: 2rem;
      background: linear-gradient(135deg, #dc3545 0%, #c82333 100%);
      color: var(--white);
      border-radius: var(--border-radius);
      font-weight: 600;
      box-shadow: var(--shadow-md);
      margin: 2rem 0;
    }

    .error-message i {
      font-size: 2rem;
      margin-bottom: 1rem;
      display: block;
    }

    /* Loading Animation */
    .loading-state {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      padding: 4rem 2rem;
      gap: 1.5rem;
    }

    .loading-spinner {
      width: 50px;
      height: 50px;
      border: 4px solid var(--gray-light);
      border-top: 4px solid var(--accent-orange);
      border-radius: 50%;
      animation: spin 1.2s linear infinite;
    }

    .loading-text {
      font-size: 1.125rem;
      color: var(--text-secondary);
      font-weight: 500;
    }

    @keyframes spin {
      0% {
        transform: rotate(0deg);
      }

      100% {
        transform: rotate(360deg);
      }
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      .page-header {
        padding: 2rem 1rem;
        margin-bottom: 1rem;
      }

      .header-content {
        flex-direction: column;
        text-align: center;
        gap: 1rem;
      }

      .header-icon {
        width: 60px;
        height: 60px;
      }

      .header-icon i {
        font-size: 1.5rem;
      }

      .header-text h1 {
        font-size: 2.25rem;
      }

      .header-text p {
        font-size: 1rem;
      }

      #mainContent {
        padding: 0 1rem 3rem;
      }

      #reviews {
        padding: 2rem 1.5rem;
        border-radius: var(--border-radius);
      }

      .reviews-stats {
        grid-template-columns: 1fr;
        gap: 1rem;
        padding: 1.5rem;
      }

      .stat-item {
        padding: 1.25rem;
      }

      .stat-number {
        font-size: 2rem;
      }

      .review {
        padding: 1.5rem;
      }

      .review-header {
        flex-direction: column;
        align-items: flex-start;
        gap: 1rem;
      }

      .review-rating {
        font-size: 1.25rem;
      }

      .review-comment {
        font-size: 1rem;
        padding: 1.25rem;
      }

      .author-avatar {
        width: 40px;
        height: 40px;
        font-size: 1rem;
      }
    }

    @media (max-width: 480px) {
      .header-text h1 {
        font-size: 2rem;
      }

      .stat-number {
        font-size: 1.75rem;
      }

      .review {
        padding: 1.25rem;
      }

      .review-comment {
        font-size: 0.95rem;
        padding: 1rem;
      }

      .empty-state {
        padding: 3rem 1.5rem;
      }

      .empty-state-icon {
        font-size: 3rem;
      }
    }

    /* Accessibility Enhancements */
    .review:focus-within {
      outline: 3px solid var(--accent-orange);
      outline-offset: 4px;
    }

    @media (prefers-reduced-motion: reduce) {

      *,
      *::before,
      *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
      }
    }

    /* High contrast mode support */
    @media (prefers-contrast: high) {
      .review {
        border: 3px solid var(--text-primary);
      }

      .review-comment {
        border-left-width: 6px;
      }

      .stat-item {
        border: 2px solid var(--text-primary);
      }
    }

    /* Print styles */
    @media print {
      .page-header {
        background: none !important;
        color: black !important;
      }

      .review {
        break-inside: avoid;
        box-shadow: none !important;
        border: 1px solid #ccc !important;
      }
    }
  </style>
</head>

<body>
  <div id="loader-placeholder" style="
      position:fixed;
      top:0; left:0; width:100vw; height:100vh;
      background:rgba(255,255,255,0.95);
      display:flex;
      flex-direction:column;
      align-items:center;
      justify-content:center;
      z-index:9999;
      transition:opacity 0.4s;
    ">
    <div style="
        border: 6px solid #e0e0e0;
        border-top: 6px solid #e85d04;
        border-radius: 50%;
        width: 48px;
        height: 48px;
        animation: spin 0.8s linear infinite;
        margin-bottom: 1.2rem;
      "></div>
    <div style="font-family: 'Poppins', sans-serif; color: #222; font-size: 1.1rem;">
      Loading, please wait...
    </div>
  </div>
  <style>
    @keyframes spin {
      to {
        transform: rotate(360deg);
      }
    }
  </style> <app-layout role="<%= role %>" username="<%= username %>" announcements='<%- JSON.stringify(LA) %>'></app-layout>

  <div class="page-header">
    <div class="header-content">
      <div class="header-icon">
        <i class="fas fa-star"></i>
      </div>
      <div class="header-text">
        <h1>My Gym Reviews</h1>
        <p>See what members are saying about your gym</p>
      </div>
    </div>
  </div>

  <main id="mainContent">
    <section id="reviews">
      <div class="reviews-stats">
        <div class="stat-item">
          <span class="stat-number" id="total-reviews">0</span>
          <span class="stat-label">Total Reviews</span>
        </div>
        <div class="stat-item">
          <span class="stat-number" id="average-rating">0.0</span>
          <span class="stat-label">Average Rating</span>
        </div>
        <div class="stat-item">
          <span class="stat-number" id="five-star-count">0</span>
          <span class="stat-label">Five Star Reviews</span>
        </div>
      </div>

      <div id="review-list"></div>

      <!-- Loading State -->
      <div id="loading-state" class="loading-state" style="display: none;">
        <div class="loading-spinner"></div>
        <div class="loading-text">Loading reviews...</div>
      </div>

      <!-- Empty State -->
      <div id="empty-state" class="empty-state" style="display: none;">
        <i class="fas fa-comments empty-state-icon"></i>
        <h3>No Reviews Yet</h3>
        <p>Your gym hasn't received any reviews yet. Encourage your members to share their experiences!</p>
      </div>
    </section>
  </main>

  <script src="../../scripts/components/app-layout.js"></script>
  <script src="../../scripts/components/map.js"></script>
  <script src="../../scripts/ownerjs/Myreviews.js"></script>
  <script>
    window.addEventListener('load', function () {
      const loader = document.getElementById('loader-placeholder');
      loader.style.opacity = 0;
      setTimeout(() => loader.style.display = 'none', 400);
    });
  </script>
</body>

</html>