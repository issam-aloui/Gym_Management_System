<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GymFit -- Dashboard</title>
    <style>
        .analytics-summary__header {
            margin-bottom: 2.5rem;
            position: relative;
            padding: 1.5rem;
            background: linear-gradient(135deg, #ffffff 0%, #f9f9f9 100%);
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
            border-left: 4px solid #e85d04;
            overflow: hidden;
            animation: fadeInUp 0.8s ease-out forwards;
        }

        .analytics-summary__header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('../../assets/static/shart.png') no-repeat;
            background-position: right center;
            background-size: contain;
            opacity: 0.05;
            z-index: 0;
        }

        .analytics-summary__header h1 {
            font-size: 2.4rem;
            margin-bottom: 0.6rem;
            color: #333;
            position: relative;
            font-weight: 700;
            background: linear-gradient(to right, #e85d04, #f48c06);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            display: inline-block;
        }

        .analytics-summary__header p {
            color: #6c757d;
            font-size: 1.1rem;
            max-width: 80%;
            position: relative;
        }

        .analytics-summary__meta {
            display: flex;
            align-items: center;
            margin-top: 1rem;
            gap: 2rem;
            position: relative;
        }

        .analytics-summary__meta-item {
            display: flex;
            align-items: center;
            color: #6c757d;
        }

        .analytics-summary__meta-item strong {
            margin-right: 0.5rem;
            color: #e85d04;
        }

        .analytics-summary__time {
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            color: #6c757d;
        }

        .analytics-summary__time::before {
            content: '';
            display: inline-block;
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background-color: #4CAF50;
            margin-right: 8px;
        }

        .analytics-summary__cards {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .analytics-summary__cards {
                grid-template-columns: 1fr;
            }

            .analytics-summary__header {
                padding: 1rem;
            }

            .analytics-summary__header h1 {
                font-size: 1.8rem;
            }

            .analytics-summary__header p {
                max-width: 100%;
            }

            .analytics-summary__meta {
                flex-direction: column;
                align-items: flex-start;
                gap: 0.5rem;
            }
        }

        /* Animation for header */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>

<body>
    <app-layout role="<%= role %>" username="<%= username %>"></app-layout>
    <main id="mainContent">
        <section class="analytics-summery">
            <div class="analytics-summary__header">
                <h1>Analytics Dashboard</h1>
                <p>Get comprehensive insights into your gym's performance metrics and member activities.</p>
                <div class="analytics-summary__meta">
                    <div class="analytics-summary__meta-item">
                        <strong>Last updated:</strong> Today at 10:30 AM
                    </div>
                    <div class="analytics-summary__meta-item">
                        <strong>Data period:</strong> Apr 1 - Apr 30, 2025
                    </div>
                    <div class="analytics-summary__time">Live data</div>
                </div>
            </div>
            <div class="analytics-summary__cards">
                <analytics-card title="Total Members" data="2,689" icon="users" icon-color="#FF9A62" icon-bg="#fff3e0"
                    trend-percentage="8.5" trend-direction="up" trend-period="last month">
                </analytics-card>

                <analytics-card title="Monthly Revenue" data="69,420 DZD" icon="revenue" icon-color="#5470FF"
                    icon-bg="#EFF1FF" trend-percentage="12.3" trend-direction="up" trend-period="last month">
                </analytics-card>

                <analytics-card title="Daily Visits" data="142" icon="visits" icon-color="#00C07F" icon-bg="#E8F5F0"
                    trend-percentage="4.2" trend-direction="up" trend-period="yesterday">
                </analytics-card>

                <analytics-card title="Active Sessions" data="38" icon="activity" icon-color="#F59E0B" icon-bg="#FEF3C7"
                    trend-percentage="2.1" trend-direction="down" trend-period="last hour">
                </analytics-card>
            </div>
        </section>
        <section class="analytics_chart">
            <div class="analytics-chart__header">
                <h2>Revenue Chart</h2>
                <p>Track your gym's revenue growth over the past months.</p>
            </div>
            <div>
                <canvas id="revenueChart" width="600" height="300"></canvas>
            </div>

        </section>
    </main>
    <script src="../../scripts/components/app-layout.js"></script>
    <script src="../../scripts/components/analytic-card.js"></script>
    <script>
        const ctx = document.getElementById('revenueChart').getContext('2d');

        new Chart(ctx, {
            type: 'line',
            data: {
                labels: ['5k', '10k', '15k', '20k', '25k', '30k', '35k', '40k', '45k', '50k', '55k', '60k', '65k', '70k', '75k', '80k', '85k', '90k', '95k', '100k', '105k', '110k'],
                datasets: [{
                    label: 'Revenue',
                    data: [20, 25, 22, 30, 50, 35, 90, 60, 70, 50, 65, 40],
                    backgroundColor: 'rgba(255, 159, 64, 0.4)',  // Light orange fill
                    borderColor: 'rgba(255, 159, 64, 1)',        // Orange border
                    borderWidth: 2,
                    fill: true,
                    tension: 0.4,  // Smooth curves
                    pointRadius: 0 // No dots
                }, {
                    label: 'chilling',
                    data: [210, 215, 221, 310, 50, 35, 90, 60, 70, 50, 65, 40],
                    backgroundColor: 'rgba(255, 159, 164, 0.4)',  // Light orange fill
                    borderColor: 'rgba(255, 159, 164, 1)',        // Orange border
                    borderWidth: 2,
                    fill: true,
                    tension: 0.4,  // Smooth curves
                    pointRadius: 0 // No dots
                }
                ]
            },
            options: {
                responsive: true,
                plugins: {
                    legend: { display: true },
                    title: {
                        display: true,
                        text: 'Revenue Chart',
                        align: 'start',
                        font: { size: 18 }
                    }
                },

                scales: {
                    y: {
                        beginAtZero: true,
                        ticks: { stepSize: 20 }
                    },
                    x: {
                        grid: { display: false }
                    }
                }
            }
        });
    </script>
</body>

</html>